<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dantefung.sample.teacher.mapper.GenericMapper">

    <resultMap id="defaultResultMap" type="hashMap">

    </resultMap>

	<!-- 単純なSQLを実行する -->
	<select id="selectCount" resultType="int" parameterType="java.lang.String">
		${_parameter}
	</select>

		<!-- 単純なSQLを実行する -->
	<select id="select" resultMap="defaultResultMap" parameterType="java.lang.String">
		${_parameter}
	</select>

</mapper>